services:
  - type: web
    name: manicure-bot
    env: python
    region: frankfurt
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
```

### 4️⃣ Загрузи обновленный `bot.py`

Скопируй код из артефакта выше и загрузи как `bot.py`

---

## 🌐 ШАГ 2: Настройка на Render.com

### 1️⃣ Зайди на сайт

Открой: https://render.com

Нажми **Sign Up** → **Continue with GitHub**

### 2️⃣ Создай новый сервис

1. Нажми **New +** (справа вверху)
2. Выбери **Web Service**

### 3️⃣ Подключи репозиторий

1. Найди свой репозиторий `manicure-bot`
2. Нажми **Connect**

### 4️⃣ Настрой параметры

Заполни поля:

**Name:** (любое имя)
```
manicure-bot-julia
```

**Region:** (выбери ближайший)
```
Frankfurt (EU Central)
```

**Branch:**
```
main
```

**Root Directory:**
```
(оставь пустым)
```

**Runtime:**
```
Python 3
```

**Build Command:**
```
pip install -r requirements.txt
```

**Start Command:**
```
python bot.py
```

**Instance Type:**
```
Free
```

### 5️⃣ Добавь переменные окружения

Прокрути вниз до раздела **Environment Variables**

Нажми **Add Environment Variable**

**Добавь 2 переменные:**

**Переменная 1:**
- Key: `BOT_TOKEN`
- Value: `8583432941:AAHA0aaAtDwCsj_Agrn6e1jDIsSNzirde6c`

**Переменная 2:**
- Key: `ADMIN_IDS`
- Value: `8485520947`

### 6️⃣ Создай сервис

Нажми **Create Web Service** внизу страницы

---

## ⏳ ШАГ 3: Ожидание деплоя

1. Render начнёт сборку (займёт 2-5 минут)
2. В логах увидишь:
```
🚀 БОТ ЗАПУЩЕН!
💅 Мастер: Юлия
📍 Адрес: г. Каменка, ул. Суворова
